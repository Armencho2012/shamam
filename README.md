
# Shamam (Frontend-only)

This repository has been converted to a simple React app (Vite) and prepared for deployment to Vercel.

How to run locally:

```bash
npm install
npm run dev
```

Build for production:

```bash
npm run build
```

To deploy on Vercel, connect this repository and Vercel will detect the `build` script and run `vite build`.
